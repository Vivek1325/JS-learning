const user_name = 'Vivek'

var id = 12 // do not use as it does not follow scope rules, it has issues with block and functional scope

// use let instead

let i = 0;

accountId = 1223 // this also works

console.table([user_name, id, i, accountId])